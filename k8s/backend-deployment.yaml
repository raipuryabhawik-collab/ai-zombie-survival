apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: game
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: docker-backend:latest
        ports:
        - containerPort: 5000

